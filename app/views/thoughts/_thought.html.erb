<p id="notice"><%= notice %></p>

<h2>
  <%= @thought.title %>
</h2>

<p>
  <%= simple_format(
  begin
    @thought.body_with_links.html_safe
  rescue 
	@thought.body
  end) %>
</p>

<%= link_to 'Thoughts', thoughts_path %><br>
<%= link_to 'Edit', edit_thought_path(@thought) %><br> 
<%= link_to 'Delete', @thought, method: :delete, data: { confirm: 'Are you sure?' } %>