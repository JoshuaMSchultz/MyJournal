%h1 Thoughts and Ideas

%table
  %thead
    %tr
      %th Ideas
      %th{:colspan => "1" }
  %tbody
    - @thoughts.each do |thought|
      %tr
        %td= link_to thought.title, thought
%br

= link_to 'Add Idea', new_thought_path
