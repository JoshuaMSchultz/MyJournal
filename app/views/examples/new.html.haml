#new-example
  = simple_form_for([@thought, @thought.examples.new]) do |form|
    #new-example-text-area
      = form.cktext_area :body, :class => "form-control", :ckeditor => {:toolbar => 'Basic'}
    = form.association :source
    = form.input :tag_list
    = form.button :submit

